package di.basic;

/*
 * 타이어를 장착해서 달릴 수 있도록 할 것이다.
 * 그런데 타이어의 브랜드가 각기 다르므로, 브랜드의 정보를 가지고오려고 한다.
 */

public class Car {
	
	private Tire tire; // 의존관계 발생
	
	// 장착된 타이어를 갖고오기만 하면 된다.
	public Car() {
		tire = new HankookTire(); // 사용자가 자동차를 주문했을 때 타이어가 장착된다.
	}
	
}
