package di.basic;

public class DriverMain {
	
	public static void main(String[] args) {
		
		Tire tire = new HankookTire(); // 외부에서 객체 생성
		Car car = new Car(tire); // 의존성이 있는 객체를 넣어주겠다
		car.prnTireBrand();
	}

}
